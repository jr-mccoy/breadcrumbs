# Open Questions

_Unresolved ambiguities and blockers. Resolve each as it is answered._
_Aged-unresolved questions are surfaced by `resume`/`audit` as "is this still open?"_

<!-- Format suggestion (one block per question):

## Q: <the question in one line>
- Opened: <YYYY-MM-DD>
- Why it matters: <impact / what is blocked>
- Needs: <human input | investigation | a decision>
- Status: open

Ask one with `crumb note question "<question>" --why … --needs …`.

Resolve one with `crumb mark-status q:<slug> answered --reason "…"` (name the
decision that answered it), or `closed` if it was withdrawn or stopped
mattering — `crumb search` prints the id to use. Only an `open` question is
listed by `crumb resume`, raises `crumb guard`'s open-blocker floor, or ages
into a staleness warning; resolved ones stay here as history and stay findable
in `crumb search`. A question with no `- Status:` bullet counts as open.
-->

_No open questions yet._
