# Open Questions

_Unresolved ambiguities and blockers. Remove or resolve each as it is answered._
_Aged-unresolved questions are surfaced by `resume`/`audit` as "is this still open?"_

## Q: Should the worker own its own schema migrations
- Opened: 2026-07-18
- Why it matters: decides whether the split can ship independently of the API deploy
- Needs: a decision
- Status: open
